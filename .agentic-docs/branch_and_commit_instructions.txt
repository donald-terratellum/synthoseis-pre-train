Git steps to start work on the decoder fix

Suggested branch name: feat/unet-decoder-fix

Run these commands locally from the repository root:

# create and switch to the branch
git checkout -b feat/unet-decoder-fix

# make your code changes (the agent will modify files in the working tree)
# e.g., edit src/synthoseis_pre_train/models.py or decoder module

# stage and commit
git add -A
git commit -m "fix(unet): replace ConvTranspose3d with Upsample+Conv3d to remove checkerboard artifacts"

# push and set upstream
git push -u origin feat/unet-decoder-fix

# After pushing, start a new agentic session to continue (clears context window)

Notes
- If you want me to create a PR, push the branch and tell me and I will prepare a PR body.
- I cannot run git commands from this agent; please run them in your shell. Once pushed, tell me and I'll continue with code edits and tests.